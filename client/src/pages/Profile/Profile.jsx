import React from 'react'
import './Profile.css'
const Profile = () => {
  // const { ready, user, setUser } = useContext(UserContext);

  return (
    <div>Profile</div>
  )
}

export default Profile